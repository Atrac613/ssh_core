name: ssh_core
description: Core building blocks for an SSH client library in Dart.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

topics:
  - ssh
  - sftp
  - networking
